package com.classroomiq.backend.entrega.domain;

/**
 * Rol de un archivo dentro de una entrega: el informe escrito o el código fuente.
 * En una entrega mixta conviven ambos roles.
 */
public enum RolArchivo {
    DOCUMENTO,
    CODIGO
}
