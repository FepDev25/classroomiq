# Pila

Implementación de una pila (LIFO) en Python con sus pruebas.

Para usarla se importa la clase `Pila` desde `pila.py` y se llaman los métodos
`apilar`, `desapilar`, `tope`, `esta_vacia` y `tamano`.
